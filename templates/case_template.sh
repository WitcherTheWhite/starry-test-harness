#!/usr/bin/env bash
set -euo pipefail

# Example test case template.
# 1. Copy to tests/<tier>/cases/<name>.sh and make executable.
# 2. Register in tests/<tier>/suite.toml under [[cases]].

echo "[$(basename "$0")] replace this with real logic"
